%# requires $account

    <select name="identity" id="identity" class="form-select" aria-describedby="identity_explain">
% foreach my $identity (sort { $a->name cmp $b->name } $account->identities) {
%   my $idid  = $identity->identityId;
    <option value="<%= $idid %>"<%= $group->isMember($idid) ? ' selected' : '' %>><%= $identity->role %></option>
% }
    </select>
